{{!-- <section class="card">
    <div class="container is-fluid my-6">
        <div class="">
            <a href="/company/{{id}}">{{company_name}}</a>
            <div class="company_name_date">
                <h1>{{user.username}}</h1>
                <p>{{position_name}}</p>
                <p>{{job_desc}} </p>
            </div>
        </div>
    </div>
</section> --}}
 <section class = "my-6">
            <div class = "box has-background-grey-darker">
               <div>
                    <div class = "has-background-grey-darker">
                        <a href="/company/{{id}}" class="title has-text-white-ter">Company Name: {{company_name}}</a>
                    </div>
                    <div class = "has-background-grey-lighter">
                        <h2 class="is-size-3">Position Name </h4>
                        <p class="is-size-4">{{position_name}}</p>
                        <h4 class="is-size-5">Job Description</h4>
                        <p>{{job_desc}}</p>
                    </div>
               </div>
                <div>
                    <p class="has-text-white-ter">Posted by {{user.username}} on {{format_date post_date}}</p>
                </div>
            </div>
      </section>